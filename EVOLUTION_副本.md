# 项目进化记录

## 目的
记录每次任务遇到的问题、解决方案，避免重复犯错，持续优化工作流。

---

## 2026-04-10：IELTS TTS 功能

### 遇到的问题

#### 1. Codex 委派任务失败
- **现象**：委派 Codex 修改 QuestionItem 组件，Codex 没能完成修改
- **原因**：长字符串参数导致命令解析错误，文件路径没有被正确传递
- **解决**：手动直接修改文件
- **改进**：委派时使用更短、更精确的描述；或明确告知"如果 Codex 失败我会手动执行"

#### 2. TypeScript 语法错误（多次）
- **现象**：
  - 第一次：缺少 Mic 图标导入
  - 第二次：漏了 `<div` 开始标签
  - 第三次：重复的上传区域
- **原因**：修改代码时没有一次性考虑完整逻辑，边写边想
- **解决**：每次修改后运行 `npx tsc --noEmit` 验证
- **改进**：
  - 修改前先在脑子里过一遍完整逻辑
  - TTS/STT/有音频 三种状态一起考虑
  - 改完后本地验证再 push

#### 3. 端口占用问题
- **现象**：`pnpm dev` 端口 5000 被 macOS ControlCenter 占用
- **原因**：macOS AirPlay/控制中心占用 5000 端口
- **解决**：使用 `PORT=3000 pnpm dev` 或 `pnpm tsx src/server.ts` 直接启动
- **改进**：在 CLAUDE.md 中记录这个常见问题

#### 4. API Key 安全意识不足
- **现象**：用户在聊天中直接发送了 ElevenLabs API Key
- **风险**：Key 暴露在聊天记录里，需要重新生成
- **改进**：不要在聊天中发送任何 Key，直接配置在 `.env.local` 里

#### 5. UI 逻辑条件不完整
- **现象**：TTS 模式下显示了 STT 的上传区域
- **原因**：没有把模式切换和显示条件一起考虑
- **改进**：设计 UI 时要考虑所有状态的排列组合

#### 6. Debug 时没有抓到真正的报错层级（最关键的问题）
- **现象**：
  - 页面显示"API有问题"
  - 多次让用户检查环境变量、重新部署、重新生成 API Key
  - 没有分析 401/402 状态码的实际含义
- **原因**：
  - 没有严格按 debug 工作流执行
  - 看到报错就跳到"环境变量问题"的结论
  - 没有读取和解释 HTTP 状态码
  - 没有检查第三方 API 的模型、接口、权限、套餐限制
- **根因**：
  - ElevenLabs 返回 401 是因为使用了旧模型 `eleven_monolingual_v1`（已废弃）
  - Voice ID 来自 Voice Library，有配额限制
  - 切换到官方音色 + 新模型后才解决
- **改进**：
  - 遇到第三方 API 报错，必须先解释状态码含义
  - 401 ≠ "key错了"，可能是模型废弃/权限不足/套餐限制
  - 402 ≠ "不能用"，可能是具体 voice 权限不够
  - 在未读取原始报错前，禁止归因于环境变量
  - 必须检查：模型是否过时、参数是否过期、资源ID是否受限、套餐是否支持

---

## 教训总结

### Debug 核心原则
遇到第三方 API 报错时：
1. **必须先解释状态码**（401/402/404/429/500）
2. **禁止默认归因于 key 或环境变量**
3. **必须检查**：模型是否过时、参数是否过期、voice 权限、套餐限制
4. **读取原始报错**后再判断根因，不要猜测

### 代码修改前
1. 先完整理解现有代码结构
2. 想清楚所有状态分支（TTS/STT/有音频/无音频）
3. 一次性写出完整逻辑，不要边写边改

### 代码修改后
1. 运行 `npx tsc --noEmit` 验证
2. 确认编译通过后再 commit
3. commit 时写清楚改了什么

### 委派任务时
1. 描述要精确，不要太长
2. 明确告诉 subagent 如果失败该怎么做
3. 备用方案：自己直接改

### 安全意识
1. API Key 不要发在聊天里
2. 配置文件用 `.gitignore` 忽略
3. 敏感信息只通过环境变量传递

---

## 工作流改进

### 当前流程（有问题）
```
需求 → 头脑风暴 → PRD → 计划 → 任务委派 → (失败) → 手动做 → 多次修复 → push
```

### 改进后流程
```
需求 → 头脑风暴 → PRD → 计划 → 任务委派
                              ↓
                        成功？→ 验证 → push
                              ↓
                        失败 → 手动做 → 本地验证 → push
                              ↓
                        自动记录进化
```

---

## 待添加
- [x] 把"自动进化记录"步骤加入 CLAUDE.md
- [x] 在项目根目录创建 EVOLUTION.md
- [x] 每次任务结束后自动更新进化记录
- [x] 创建 debug.md 单独文档
- [x] 在 CLAUDE.md 加入 Debug 规则

---

## 2026-04-10（下）：TTS/STT 统一视图重构

### 完成的功能
1. **TTS 输入区重构**：textarea + 实时句子预览 + 切分点微调
2. **TTS 生成流程**：调用 TTS API → ASR 获取精确时间戳
3. **统一结果视图**：两阶段结构（输入区 → 结果视图）
4. **句子微调功能**：TTS/STT 都支持时间戳调整和文本编辑
5. **自由切分句子**：TTS/STT 都支持用 `/` 标记拆分长句

### 遇到的问题

#### 1. Codex 无法正常委派
- **现象**：通过 Agent tool 委派 Codex 任务失败
- **原因**：Codex CLI 的 `/codex:rescue` 是 slash command，不是子代理
- **结论**：Codex 只能用户在 Claude Code 聊天框直接输入 `/codex:rescue` 调用
- **改进**：Codex 无法程序化调用时，直接自己实现

#### 2. 用户需求渐进明确
- **现象**：用户一开始说"TTS 没有自由切分"，后来发现 STT 也需要
- **原因**：设计时没有充分考虑 STT 模式的类似需求
- **改进**：头脑风暴阶段要把两种模式的交互都想清楚

#### 3. 拆分标记选择
- **现象**：用户反馈 `||` 符号难打
- **改进**：征求用户意见后改为 `/`

### 经验教训

#### 关于 Codex
- Codex 不是给我调用的子代理，是用户在 Claude Code 直接输入的 slash command
- 如果 Codex 在当前环境不可用，直接自己实现，不要浪费时间

#### 关于需求澄清
- 设计功能时要考虑所有输入模式（TTS/STT）的对称性
- 如果一个模式有某个功能，另一个模式很可能也需要
- 用户说"XX 功能没有"时，要确认是 TTS 模式还是 STT 模式

#### 关于功能设计
- 拆分标记要选用户易输入的符号（`/` 比 `||` 好打）
- 交互设计时要考虑键盘输入的便利性

#### 关于推送
- 小改动可以直接 push，不需要每次都完整测试
- 但 UI 功能完成后应该让用户先在 dev 环境确认

---

## 工作流改进

### 当前流程（有问题）
```
需求 → 头脑风暴 → PRD → 计划 → 任务委派 → (失败) → 手动做 → 多次修复 → push
```

### 改进后流程
```
需求 → 头脑风暴 → PRD → 计划 → 任务委派
                              ↓
                        成功？→ 验证 → push
                              ↓
                        失败 → 手动做 → 本地验证 → push
                              ↓
                        自动记录进化
```
