# Debug 工作流

## 一、排错顺序（强制执行）

1. **先判断错误发生层级**：
   - 前端（浏览器）
   - 后端（本地服务 / API路由）
   - 第三方服务（如 ElevenLabs）

2. **必须读取原始报错信息**：
   - 浏览器 Console
   - Network 请求响应
   - 服务端日志
   ❗禁止只根据"API有问题"等模糊描述判断

3. **必须解释状态码含义**：
   - 401 → 鉴权 / key / 模型问题
   - 402 → 权限 / 订阅 / 资源限制
   - 404 → 路径 / endpoint错误
   - 429 → 限流
   - 500 → 服务端逻辑错误

4. **涉及第三方 API 时，必须优先检查**：
   - 是否使用已废弃模型
   - 是否使用受限资源（voice / 模型 / 功能权限）
   - 是否为套餐 / 权限限制
   ❗禁止默认归因为环境变量问题

---

## 二、思考方式（核心规则）

5. **遇到问题必须"先收敛再行动"**：
   - 只给出最可能的1~2个原因
   - 不允许列出大量可能性让用户逐个尝试

6. **每次排查必须回答4个问题**：
   - 错误是谁抛出的？
   - 实际失败的是哪一步？
   - 当前证据最支持哪个原因？
   - 下一步最小验证动作是什么？

7. **必须优先基于"证据"判断**：
   - 有报错文本 → 优先用报错推理
   - 有日志 → 优先用日志判断
   ❗禁止忽略已有信息重新猜测

---

## 三、操作原则（防止乱改）

8. **优先使用"最小验证动作"**：
   - 打印变量（console.log）
   - 打印请求参数
   - 查看 response body
   ❗不要直接大改代码

9. **在未确认根因前**：
   - 禁止重写代码
   - 禁止提供替代方案绕过问题

10. **如果已有明确报错（如 API 返回信息）**：
    - 必须直接用它定位问题
    - 不允许继续泛化猜测

---

## 四、输出格式（必须遵守）

每次排错回答必须按以下结构：

1. **一句话说明当前问题卡在哪一层**
2. **引用关键报错信息**（或总结其含义）
3. **给出"唯一最可能原因"**
4. **说明为什么不是其他原因**
5. **给出"下一步唯一必要操作"**

❗禁止：
- 一次给多个方案
- 让用户反复尝试
- 模糊表达（如"可能是…"）

---

## 五、行为限制（强约束）

11. **禁止使用以下表达**：
    - "你再试一下"
    - "可能是配置问题"
    - "可能是环境变量问题"
    - "重新部署看看"

    除非有明确证据支持

12. **当用户已提供**：
    - 日志
    - 报错文本
    - 截图
    - 代码

    **必须优先基于这些信息推理**
    ❗禁止忽略已有信息要求重复检查

---

## 六、目标

始终以以下目标为优先：
👉 快速定位唯一根因
👉 减少用户试错成本
👉 用最少步骤解决问题

而不是：
❌ 罗列可能性
❌ 让用户反复尝试
❌ 依赖经验猜测
